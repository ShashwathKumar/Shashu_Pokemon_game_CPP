/****************************************************************************
******************                                       ********************
**********   It  contains the function   `line_dan'  ************************
******************                                      *********************
****************************************************************************/

void line_dan()
{
   int x1=0,y1=0,x2=0,y2=0;
    setbkcolor(0);
    //randomize();

/////////////////////////////////////////////////////////////////////////////
/////////                                                          //////////
///////// LOOP TO SLIDE LINES OVER  __ X-AXES __ ......            //////////
/////////                                                          //////////
/////////////////////////////////////////////////////////////////////////////

for(int i=0; i<2; ++i)
{
 setcolor(BLACK);
 x1=0,y1=0,x2=0,y2=0;

 for(; x1<=640; ++x1,--x2)
 {
   int xmax, ymax;

   randomize();
   if(i==0) if(x1==0) setcolor(GREEN);
   xmax = getmaxx();
   ymax = getmaxy();

   /* draw a diagonal line */
   line(0+x1, 0+y1, xmax+x2, ymax+y2);
   delay(3);
 }

/////////////////////////////////////////////////////////////////////////////
//////// Loop to slide on x-axes ends                          //////////////
/////////////////////////////////////////////////////////////////////////////


/////////////////////////////////////////////////////////////////////////////
/////////                                                          //////////
/////////    LOOP TO THE LINES OVER  Y- AXES ............          //////////
/////////                                                          //////////
/////////////////////////////////////////////////////////////////////////////

 for(; y1<=480; ++y1,--y2)
 {
   int xmax, ymax;

   randomize();
   if(i==0) if(y1==0) setcolor(CYAN);
   xmax = getmaxx();
   ymax = getmaxy();

   line(0+x1, 0+y1, xmax+x2, ymax+y2);

   delay(3);
 }
/////////////////////////////////////////////////////////////////////////////
//////// Loop to slide on y-axes ends                          //////////////
/////////////////////////////////////////////////////////////////////////////
}
delay(250);
cleardevice();
}
